/* General body styling */
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f0f4f8;
    color: #333;
    margin: 20px;
    padding: 0;
}

/* ID selector for the main heading */
#main-title {
    color: #0077cc;
    text-align: center;
    margin-bottom: 20px;
}

/* Class selector for paragraph styling */
.description {
    font-size: 1.1rem;
    line-height: 1.6;
    padding: 10px;
    background-color: #ffffff;
    border-left: 5px solid #0077cc;
    margin-bottom: 20px;
}

/* Tag selector for all images */
img {
    max-width: 100%;
    height: auto;
    border: 4px solid #ccc;
    padding: 5px;
    border-radius: 8px;
    display: block;
    margin: 0 auto 20px;
}
